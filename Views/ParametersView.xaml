<Window x:Class="GameStoreWPF.Views.ParametersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameStoreWPF.Views"
        mc:Ignorable="d"
        Title="Game Parameter" Height="160" Width="250" Background="#17181a">
    <StackPanel Width="180">
        <Label Content="Video game parameter" HorizontalAlignment="Center" FontWeight="Bold" FontSize="13" Foreground="White" Margin="0 0 0 5"/>
        <TextBox Height="25" Text="{Binding Parameter, Mode=TwoWay}" FontSize="15"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Style="{StaticResource HoverButtonStyle}" Content="Continue" x:Name="Continue" />
            <Button Style="{StaticResource HoverButtonStyle}" Content="Cancel" x:Name="Cancel" />
        </StackPanel>
    </StackPanel>
</Window>
